language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t hageotoko/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true hageotoko/docker-react npm run test
